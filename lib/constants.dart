import 'package:flutter/material.dart';

// ブランドカラー4色
const deepBlue = Color(0xFF0D47A1); // 信頼 — サイドメニュー背景、強い強調見出し
const primaryColor = Color(0xFF1E88E5); // メイン — ボタン・アイコン・選択中の強調
const skyBlue = Color(0xFF4FC3F7); // 親しみ — サブ見出し・補助アイコン・ホバー
const iceBlue = Color(0xFFE3F2FD); // アクセント — カード背景・区切り・ハイライト

const bgColor = Colors.white;
const textColor = Color(0xFF1A1A1A);
const subTextColor = Color(0xFF5C6B7A);

const defaultPadding = 16.0;
